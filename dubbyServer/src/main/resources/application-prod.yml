spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

server:
  forward-headers-strategy: framework   # 리버스 프록시(TLS 종료) 뒤 배치 가정

management:
  endpoints:
    web:
      exposure:
        include: health
